#include<stdio.h>
#include<math.h>
int main()
{                                  
    float a,b;
    printf("enter the base element :",a);
    scanf("%f",&a);
    printf("enter the index element :",b);
    scanf("%f",&b);
    printf("the power is :%f",pow(a,b));
    return 0;
}